#include<stdio.h>
int main(){
  int a, b, c, sum;
  printf("Enter the values of a, b, c:\n");
  scanf("%d %d %d", &a, &b, &c);
   sum=(a+b+c)/3;
   
  
    printf("%d",sum);

    return 0;
}
